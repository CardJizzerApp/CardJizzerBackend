{
  "name": "cah-backend",
  "version": "0.0.2",
  "description": "Backend for CAH.",
  "main": "index.js",
  "scripts": {
    "setup": "git config core.hooksPath hooks",
    "dev": "npm run setup && node ./src/server.js",
    "test": "npm run setup && mocha ./src/*.spec.ts  --timeout 7000",
    "lint": "npm run setup && eslint src"
  },
  "author": "IJustDev",
  "license": "ISC",
  "dependencies": {
    "cardcast-api": "^0.2.3",
    "express": "^4.17.1",
    "express-ws": "^4.0.0",
    "uuid": "^3.3.2",
    "ws": "^7.1.2"
  },
  "devDependencies": {
    "chai": "^4.2.0",
    "eslint": "^6.2.1",
    "eslint-config-google": "^0.13.0",
    "jshint": "^2.10.2",
    "mocha": "^6.2.0"
  }
}
